<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="index,follow">
  <title>{{ title }} | {{ metadata.title }}</title>
  <link rel="icon" href="{{ metadata.favicon | url }}" type="image/png">
  <link rel="stylesheet" href="{{ '/css/fonts.css' | url }}">
  <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
  
  {% if description %}
    <meta name="description" content="{{ description | safe }}">
  {% endif %}
  {% seo "" %}
  {# {% canonicalURL /feed.xml %} #}
  <!-- Fathom - beautiful, simple website analytics -->
  {% if (environmentVariables.environment == 'production') %}
    <script src="https://cdn.usefathom.com/script.js" data-site="PJHXPNYF" defer></script>
    <script async defer data-domain="playthistonight.com" src="https://plausible.io/js/plausible.js"></script>
  {% endif %}
  <!-- / Fathom -->
</head>