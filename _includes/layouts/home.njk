--- 
  layout: layouts/base.njk 
  section: home     
---

<h1>Latest Post</h1>
<div class="flex-grid flex-grid--wrap w-100">
    {% set post = collections.posts | sort(attribute = "date") | last %}
    <a class="flex-grid__item flex-grid__item--full-width" href="{{ post.url | url }}">
        <h2 class="home-title">{{ post.data.title }}</h2>
        <p>
            <time datetime="{{ post.data.date | machinedate }}">
                <small>{{ post.data.date | readableDate }} by {{ post.data.author }}</small>
            </time>
        </p>
        {% set image = post.data.content | getFirst("img") %}
        {% if image %}
            <img src="{{image.src}}" alt="{{image.alt}}"/>
        {% endif %}
        {% if post.data.summary %}
            <p class="home-summary">
                {{ post.data.summary }}
            </p>
        {% endif %}
        <small>Read More ></small>
    </a>
</div>

<div class="flex-grid t-center">
    <a class="flex-grid__item" href="/blog">
        <h2 class="home-title m-0">All Posts →</h2>
    </a>
</div>
