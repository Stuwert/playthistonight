<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

    {% include "components/head-post.njk" %}

    <body>

        {% include "components/header.njk" %}

        {{ layoutContent | safe }}

        {% if (writingType == 'review') %}
            {% include "components/moreArticles.njk" %}
        {% endif %}

        {% include "components/footer.njk" %}

    </body>

</html>
